/* Implementar por código una multiplicación con enteros de 8 bits (emulando lo realizado en la
práctica de Assembly).
-------------------------------------------------------------------------------------------------
Alumno: Javier Ceferino Rodriguez */

/*---- Includes ----*/
#include <stdio.h>

/*---- Principal ----*/
int main (void){
    // Con enteros decimales
    int a = 4, b = 5, resu = 0;

    for (int i=0; i<b; i++){
        resu += a;
    }
    printf("Resultado: %d\n", resu);

    // Con enteros binarios
    // jaj salu2
}